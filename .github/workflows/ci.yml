name: CI

on:
  pull_request:
    branches: [dev, staging, prod]

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - run: npm ci
      - run: npx prettier --check .
      - run: npm run lint
      - run: npm run typecheck

      - name: Build the production image
        run: |
          touch .env
          docker build --target prod --secret id=env,src=.env .
